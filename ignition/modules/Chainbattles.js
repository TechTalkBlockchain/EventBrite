const { buildModule } = require('@nomicfoundation/ignition-core');

module.exports = buildModule('ChainBattlesModule', (m) => {
  const chainBattles = m.contract('ChainBattles');  // Define the contract
});
